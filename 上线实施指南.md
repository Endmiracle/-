# 羽衣电竞陪玩制度问答 · 上线实施指南

> 路线：**微信云托管（Cloud Hosting）+ 体验版小程序** · 镜像采用 **代码部署（GitHub，路径 B）**
> 适用对象：已有小程序 AppID、想把「自建 RAG 引擎 + 陪玩制度问答」真正跑给真机用户用的人。
> 文档与代码同源，所有配置项、字段名、路径均可在工程内核对（`server/config.js`、`server/Dockerfile`、`miniprogram/utils/config.js`、`miniprogram/pages/settings/`）。

---

## 0. 这条路线能给你什么 / 代价是什么

**架构一句话**：小程序不直连任何服务器，而是通过 `wx.cloud.callContainer` 走微信内网调用你的 Docker 容器（RAG 引擎）。

| 好处 | 代价（务必提前知道） |
|---|---|
| 不需要买服务器、配域名、做 ICP 备案 | 云托管 **没有流式输出**：问答要等服务端全部生成完一次性返回（已在客户端强制 `stream:false`） |
| 不需要在小程序里配「request 合法域名」 | 单次调用 **超时上限 15 秒**（官方限制） |
| 不需要在客户端存任何密钥 / 令牌 | 前端「停止生成」按钮在云托管模式下不可用 |
| 访问控制交给微信网关（openid 白名单） | 冷启动 / 扩缩容时容器文件系统不持久，靠自动 seed 重建知识库 |

> 如果业务对「逐字流式」强需求，请改走「自建服务器 + HTTPS 域名」路线（见 README 第二节），本指南不展开。

---

## 1. 前置清单

- [ ] 小程序 **AppID**（非 `touristappid`）：在 `project.config.json` 里把 `"appid": "touristappid"` 换成你自己的。
- [ ] 该小程序已开通 **微信云开发** 环境，并记下 **环境 ID**（形如 `cloud1-xxxxxx`）。
- [ ] 云托管（容器）能力可用：建议 **企业/组织认证主体**；个人主体部分容器能力可能受限。
- [x] **不需要本地 Docker**：本项目采用「代码部署」，代码已在 GitHub 仓库 `Endmiracle/-` 的 `main` 分支（路径 B）。
- [ ] DeepSeek API Key（已配好，默认 `sk-...`；**只填到云托管控制台的环境变量里，不要写进仓库**）。

---

## 2. 后端部署到微信云托管

### 2.1 准备镜像

工程已带 `Dockerfile`（零第三方依赖，无需 `npm install`，镜像小、构建快），默认监听 **80** 端口并开启自动 seed 与 `/health` 健康检查。

**路径 A（推荐，本地构建后推送 TCR）**
```bash
# 在 yuyi-esports-qa 根目录
docker build -t yuyi-rag:latest .
# 推到腾讯云容器镜像服务 TCR（或云托管自带镜像仓库），示例：
docker tag yuyi-rag:latest ccr.ccs.tencentyun.com/<你的命名空间>/yuyi-rag:latest
docker push ccr.ccs.tencentyun.com/<你的命名空间>/yuyi-rag:latest
```

**路径 B（✅ 本项目采用：代码部署，免本地 Docker）**

把代码托管到 Git 仓库，云托管按 `Dockerfile` 自动构建。本项目已配好 SSH 并推送至：

- 仓库：`git@github.com:Endmiracle/-.git`，分支 **`main`**

换机器 / 重装时复现步骤：

```bash
# 1) 初始化（.gitignore 已排除密钥与运行时数据，可放心 add -A）
git init && git add -A && git commit -m "init"

# 2) 设置提交身份（否则作者会显示为占位名）
git config user.name  "Endmiracle"
git config user.email "你的GitHub邮箱"

# 3) 配置 SSH 凭证（ed25519，空口令便于自动化）
ssh-keygen -t ed25519 -C "你的GitHub邮箱" -f "$HOME/.ssh/id_ed25519" -N ""
# 把 ~/.ssh/id_ed25519.pub 内容粘贴到 GitHub → Settings → SSH and GPG keys
ssh -T git@github.com          # 出现 Hi <用户名>! 即成功

# 4) 关联远端并推送（分支用 main，与 GitHub 默认一致）
git remote add origin git@github.com:<用户名>/<仓库名>.git
git branch -M main
git push -u origin main
```

> ⚠️ 密钥与运行时数据绝不入库：`.gitignore` 已排除 `server/.env`、`server/data/`、`*.pem/*.key/*.crt`；`.dockerignore` 另行排除 `server/data/`，确保镜像内 data 为空、从而触发 `AUTO_SEED` 自动建库。（注：若远端已有自动生成的 `Initial commit`，首次可用 `git push -f origin main` 覆盖。）

### 2.2 在云开发控制台创建云托管服务

1. 进入 **微信云开发控制台** → **云托管** → **新建服务**。
2. 服务名称建议用 `yuyi-rag`（后面小程序填的「云托管服务名」就是这个，**不是容器名**）。
3. 部署方式选「代码」(路径 B)：授权 GitHub → 选中仓库 `Endmiracle/-` → **分支选 `main`**；平台会按 `Dockerfile` 自动构建（无需本地 Docker）。
4. 部署完成后，在 **服务设置** 里确认：
   - **端口**：`80`（与 `Dockerfile` 的 `ENV PORT=80` 一致）
   - **健康检查**：路径 `/health`，返回 `code:0` 即健康
   - **公网访问**：保持关闭即可（小程序走内网 `callContainer`，不需要公网域名，也因此无需备案）

### 2.3 配置环境变量（关键）

在云托管服务 **环境变量** 页面添加（大小写敏感，与 `server/config.js` 的 `raw()` 一一对应）：

| 变量名 | 取值 | 说明 |
|---|---|---|
| `LLM_BASE_URL` | `https://api.deepseek.com/v1` | 默认即此，可省略 |
| `LLM_API_KEY` | `sk-你的DeepSeekKey` | **只在这里填**，绝不入仓 |
| `LLM_MODEL` | `deepseek-chat` | 默认即此，可省略 |
| `LLM_TEMPERATURE` | `0.2` | 制度问答偏严谨，调低可减少编造 |
| `ALLOW_OPENIDS` | `oXXXX你的openid` | openid 白名单，逗号分隔；**留空 = 完全开放**（仅调试用） |
| `AUTO_SEED` | `true` | 知识库为空时自动灌 `server/knowledge/`，`Dockerfile` 已设，可省略 |
| `PORT` | `80` | `Dockerfile` 已设，可省略 |
| `HOST` | `0.0.0.0` | `Dockerfile` 已设，可省略 |

> 改完环境变量后点「重新部署 / 更新」，让新配置生效。

### 2.4 确认知识库已自动建好

容器启动后，`server/index.js` 的 `start()` 会检测到知识库为空并自动 `seed.run()`，把 `server/knowledge/` 下的制度正文（基本制度.md、羽衣电竞陪玩制度.md）导入并建立知识库「羽衣电竞陪玩制度」（`meta.json` 是配置，不作为独立文档；实际日志为 `2/2 文档`）。

验证方式（任选其一）：
- 云托管控制台「日志」里应看到 `[start] 导入完成：N/M 文档，K 切片`；
- 或本地临时跑一次 `npm run seed`（会连本地库，仅用于核对素材是否齐全）。

> 扩缩容 / 重启后容器回到镜像初始状态、数据清空，知识库会自动重建，无需人工干预。

---

## 3. 拿到你的 openid 填白名单（可选但推荐）

`ALLOW_OPENIDS` 是「谁能用这个问答」的开关。有两个取法：

**法一：临时在小程序里打印（调试阶段最方便）**
在任意页面 `onLoad` 里加一句 `wx.cloud.callFunction` 或直接 `console.log(wx.getStorageSync('...'))` 不直观；更稳的是用云函数：
```js
// 云函数 getOpenid
exports.main = async (event, context) => ({ openid: event.userInfo.openId });
```
在小程序调用后把返回的 openid 填进 `ALLOW_OPENIDS` 即可。

**法二：先留空跑通，再收紧**
上线初期把 `ALLOW_OPENIDS` 留空（此时任何能调到的请求都放行），等体验成员稳定后再逐个填入，改为白名单模式。

> 注意：配了白名单后，**只有名单内的微信账号**能问答；名单外的请求会收到 `401` 与明确提示「请让管理员把你的 openid 加入 ALLOW_OPENIDS」。

---

## 4. 小程序端切到云托管模式

### 4.1 导入并绑定 AppID
1. 微信开发者工具 → **导入项目** → 选择 `yuyi-esports-qa/` 目录。
2. 把 `project.config.json` 的 `"appid": "touristappid"` 改成你的真实 AppID。
3. 工具右下角确认已关联同一个 **云开发环境**（环境 ID 与第 2 步一致）。

### 4.2 在设置页切换（无需改代码）
打开小程序 **「设置」页** → 连接模式选择 **「云托管」** → 填写两项：
- **云环境 ID**：你的环境 ID（如 `cloud1-xxxxxx`）
- **云托管服务名**：`yuyi-rag`（第 2.2 步建的服务名称）

保存后系统会提示「**改这两项后要完全退出小程序再进才会生效**」。退回重进即可。

> 底层逻辑（`miniprogram/utils/config.js` 的 `resolve()`）：云模式下 `useStream()` 恒为 `false`，请求经 `miniprogram/utils/cloud.js` 的 `wx.cloud.callContainer` 走内网，已自动带上 `X-WX-SERVICE` 头。

### 4.3 开发者工具里的开关
- 调试基础库建议 **2.13.1 以上**（低于此版本 `wx.cloud` 不可用）。
- 云托管走内网，**不需要**勾选「不校验合法域名」，也**不需要**备案。

---

## 5. 发布「体验版」

体验版无需微信审核，扫码即用，适合内部陪玩师先跑起来：

1. 微信开发者工具 → 右上角 **上传** → 填版本号与备注 → 上传成功。
2. 进入 **小程序后台（mp.weixin.qq.com）→ 管理 → 版本管理 → 体验版** → 点击「设置为体验版」。
3. 同一页 **成员管理 → 添加体验者** → 输入陪玩师 / 管理员的微信号 → 对方在微信里收到邀请，扫码进入体验版。
4. 体验者在自己微信里打开该小程序，按第 4.2 步填好云环境 ID 与服务名（或你已把默认值写进 `DEFAULT_CONFIG`），即可提问。

> 体验版与正式版代码同源；后续要上正式版再走「提交审核 → 发布」流程，配置不变。

---

## 6. 上线前自检 & 验证

**后端自检（控制台 / 日志）**
- [ ] 容器状态「运行中」，健康检查 `/health` 返回 `code:0`
- [ ] 日志出现 `[start] 导入完成：2/2 文档，34 切片`
- [ ] 环境变量 `LLM_API_KEY` 已设、`ALLOW_OPENIDS` 符合预期

**小程序自检（设置页「开始自检」）**
- [ ] 云环境 ID / 服务名 校验通过
- [ ] 连通性自检全绿（走 `wx.cloud.callContainer` 调 `/api/v1/chats` 等）

**问答验证（真机 / 模拟器）**
- [ ] 问「炸单扣多少钱」→ 返回金额并带引用来源（制度文档名 + 小节）
- [ ] 问「客服链接在哪里」→ 命中客服相关片段
- [ ] 问知识库没有的内容 → 回答「制度里没有这条」而非编造
- [ ] 连续两轮对话上下文衔接正常

> 自动化回归：`npm test`（引擎 + 客户端共 **113** 项断言，含云托管通道 18 项）可在上线前本地跑一遍确认无回归。

---

## 7. 运维与排障

**密钥安全**
- `LLM_API_KEY` 只在云托管控制台环境变量里，仓库 `.env` 已被 gitignore / dockerignore 排除。
- 若密钥曾在聊天中出现，建议到 DeepSeek 平台重新生成一个再更新环境变量。

**15 秒超时**
- 云托管硬性上限。若偶发超时：换响应更快的大模型、或在助手配置里调小 `topK`、关闭向量检索（纯 BM25 更快）。
- 客户端已把超时夹在 3s~15s 之间（`miniprogram/utils/cloud.js` 的 `clampTimeout`），超时会给出可读提示「等待超过 15 秒，可换更快的大模型或减少 Top-K」。

**常见错误码（`cloud.js` 已归一化为人话）**
| 现象 | 含义 / 处理 |
|---|---|
| `-501000` / 含 `env` | 云环境 ID 不对或小程序未关联该环境，到设置页核对 |
| 含 `service` / `404` | 云托管服务名不对，到「云托管 → 服务列表」核对后重填 |
| `timeout` | 见上「15 秒超时」 |
| `401` | openid 不在 `ALLOW_OPENIDS` 白名单 |
| `未初始化` | 改了云环境 ID 后没「完全退出小程序再进」 |

**扩缩容 / 数据不持久**
- 容器文件系统不持久，但 `AUTO_SEED=true` 会在空库时自动重建知识库（约几秒~十几秒），期间首个请求稍慢属正常。
- 若要**更新制度文档**：改 `server/knowledge/` 后重新构建镜像并部署；或进容器手动执行 `node scripts/seed.js --force` 重建（生产环境推荐前者，可审计、可回滚）。

---

## 8. 知识库更新与回滚

> ⚠️ 核心认知：制度文件在**镜像内**（`Dockerfile` 里 `COPY server/`），且容器文件系统**不持久**（重启 / 扩缩容回到镜像初始状态）。所以「更新知识库 = 改文件 + 让新镜像生效」。

### 8.1 标准更新流程（推荐）
1. 编辑 `server/knowledge/` 下的制度正文（`基本制度.md`、`羽衣电竞陪玩制度.md`；`meta.json` 是配置，一般不动）。
2. 本地跑 `npm run eval` 验收检索召回（确认新内容能被命中）。
3. `git add -A && git commit -m "更新制度：xxx" && git push`（推到 GitHub `main`）。
4. 云托管控制台 → 该服务 → **「部署发布」→「发布」**（平台按最新代码重新构建镜像）。
5. 新容器启动自动 `AUTO_SEED` 重建知识库；日志出现 `[start] 导入完成：2/2 文档，N 切片` 即生效。

### 8.2 临时改（不推荐，重启即丢）
- 云托管控制台 → 服务 → 「云端调试」进入容器终端 → `node scripts/seed.js --force`。
- 只对当前实例生效，**重启/扩缩容后恢复为镜像内容**，仅适合临时验证。

### 8.3 ⚠️ 两个必须知道的限制
- **小程序内的「知识库管理」上传，在云托管模式下不持久**：上传文件存容器本地 `server/data/`，重启即丢。正式更新请走 8.1。
- **重新发布 / 扩缩容会清空会话记录**（容器不持久），这是该路线的固有代价。

### 8.4 想让知识库「不改镜像也能更新」？
需架构改造：把知识库 / 索引落到**对象存储或云数据库**（持久化），服务从那里加载。若制度更新很频繁，值得做——需要时我帮你评估改造点。

**回滚**
- 云托管服务「版本」列表里选上一个版本「回滚」即可，无需改小程序。
- 小程序侧若改过 `DEFAULT_CONFIG` 默认值，回退对应提交后重新「上传」体验版。

---

## 9. 一页速查（上线当天的操作顺序）

1. `project.config.json` 改真实 AppID；确认云开发环境 ID。
2. 代码部署（路径 B）：确认最新代码已 `git push` 到 GitHub 仓库 `Endmiracle/-` 的 `main` 分支。
3. 云托管建服务 `yuyi-rag`，端口 80，健康检查 `/health`，关公网。
4. 环境变量：`LLM_API_KEY` + `ALLOW_OPENIDS`（先留空也可）+ `AUTO_SEED=true` → 重新部署。
5. 看日志确认「导入完成：2/2 文档」。
6. 开发者工具导入项目 → 设置页选「云托管」→ 填环境 ID + 服务名 → 退出重进。
7. 真机问「炸单扣多少钱」验证引用来源。
8. 开发者工具「上传」→ 后台设「体验版」→ 加体验者微信号 → 交付。
